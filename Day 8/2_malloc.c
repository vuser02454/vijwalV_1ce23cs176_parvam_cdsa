#include<stdio.>
#include<stdlib>
int main(){
    int n;
   Printf("enter the number of elements: ");
   scanf("%d",&n);
   int* array=(int*)malloc(n * sizeof);
   if (array== NULL )
   {
    printf("memory allocation failed!\n")
    return 1;
   }
   printf("enter %d elements:\n",n);
   for(int i=0; i<n; i++){
    scanf("%d",&array[i]);
   }
   printf("you enetered:");
   for(int i)


    int main{
        printf("you enetered:");
        for(int i=0; i<n; i++)
    {
        printf("%d",array[i]);

    }
   }